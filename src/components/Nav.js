import {Component} from "react";

class Nav extends Component {
    render() {

        return (
            <nav className={"App-Nav"}>
                <div className={"App-list"}>

                </div>
            </nav>
        );
    }
}

export default Nav;
